node_modules/
.next/
dist/
out/
build/
.env.local
.env*.local
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

# ── Editor / OS noise ──
.vscode/
.idea/
*.swp
*~

# ── Service worker dev caches ──
# Phase 11.2 ships public/sw.js; the SW itself IS committed. These are
# build-time / runtime caches generated by the browser, never by us.
.workbox/
sw-precache-*.js

# ── Phase 12.1 — Sentry source maps ──
**/*.map
.sentryclirc
